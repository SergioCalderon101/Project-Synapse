# =============================================================================
# CONFIGURACIÓN DE SYNAPSE AI
# =============================================================================
# Copia este archivo a .env y completa los valores

# =============================================================================
# OpenAI API Configuration
# =============================================================================
# REQUERIDO: Tu API key de OpenAI (obtén una en https://platform.openai.com/api-keys)
OPENAI_APIKEY=sk-your-api-key-here

# Modelo para chat (default: gpt-3.5-turbo)
# Opciones: gpt-3.5-turbo, gpt-4o, gpt-4, gpt-4o-mini
OPENAI_CHAT_MODEL=gpt-3.5-turbo

# Modelo para generación de títulos (default: gpt-3.5-turbo)
OPENAI_TITLE_MODEL=gpt-3.5-turbo

# =============================================================================
# Flask Configuration
# =============================================================================
# Modo debug (True/False) - SIEMPRE False en producción
FLASK_DEBUG=True

# Puerto del servidor (default: 5000)
PORT=5000

# Host del servidor (default: 0.0.0.0)
HOST=0.0.0.0

# =============================================================================
# CORS Configuration
# =============================================================================
# Orígenes permitidos para CORS (separados por coma)
# Para desarrollo local usa "*"
# Para producción especifica dominios: https://tudominio.com,https://app.tudominio.com
CORS_ORIGINS=*

# =============================================================================
# Logging Configuration
# =============================================================================
# Nivel de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Para desarrollo: DEBUG o INFO
# Para producción: WARNING o ERROR
LOG_LEVEL=INFO
